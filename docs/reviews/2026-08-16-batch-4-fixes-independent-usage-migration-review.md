## must-fix

**Verdict: FAIL — 4 must-fix, 3 should-fix.** Reviewed `55f092920f2ac4f972270d07b9328de3f9bb5d79` against revised-plan baseline `aaad446`. Required gates passed in order: `npm install`, `npm run build -w @agentos/web`, `npm test`, and `npm run typecheck` all exited 0. On Node `v26.5.0`, `npm run test:db` rebuilt only the dedicated `agentos_test` schema and passed 109/109; no reported Node-26 schema collision reproduced. The lock-deletion check was run in a detached disposable worktree: with the advisory-lock statement removed, focused tests 1 and 2b both failed exactly as intended (`stored 10`, and the contender resolved). All scratch schemas/worktrees used below were removed.

1. **[must-fix | Feasibility + focused feasibility — a lock timeout can still make the final cache permanently stale.]** The transaction installs a 3-second `lock_timeout` for the whole recompute at `packages/db/src/usage.ts:304-313`, then reads the terminal events and writes an absolute value at `:315-327`. The online path suppresses every recompute error and still acknowledges the event batch at `packages/api/src/app.ts:2519-2529`; the backfill uses the same recompute at `packages/db/src/usage.ts:358-375`. A real-PostgreSQL fixture interleaving reproduced the uncovered case: backfill A acquired the session lock and read event 1 (`10`), event 2 (`20`) was inserted, online recompute B waited and failed after 3.107 seconds with Prisma `P2010` wrapping PostgreSQL `55P03`, then A committed. Probe result: `{"eventTotal":30,"storedInputTokens":10,"onlineOutcome":{"code":"P2010"},"oldWriterOutcome":"resolved"}`. There need be no later terminal event, so the catch's promised “next FINAL_OUTPUT or backfill” repair does not exist; the second production backfill can suffer the same last-window interleaving. **Remedy:** add this >3-second ingest/backfill test, and make a timed-out contender leave a durable repair obligation. One workable no-stale-write protocol is for every successful absolute writer to return the maximum event `seq` it folded, re-read that watermark after commit, and loop under the lock when it changed; any failed post-check must enqueue/persist a session-scoped retry rather than be treated as clean. A durable `usageDirty`/watermark queue is also valid. Merely retrying a fixed number of times in the request is not a correctness guarantee.

2. **[must-fix | Feasibility + migration safety — the documented `psql "$DATABASE_URL"` commands do not accept the repository's Prisma URL shape.]** The runbook sends `DATABASE_URL` directly to psql in preflight and index creation (`docs/runbooks/batch-4-rollback.md:31-38`, `:54-57`) and again for rollback (`:222-250`), while explicitly saying that repository URLs carry Prisma's `?schema=...` parameter at `:101-114`; `packages/api/src/testdb.ts:6-10` is a concrete such URL. PostgreSQL 16/libpq does not recognize that Prisma-only parameter. Exact fixture probe: `psql '.../agentos?schema=agentos_test' -c 'SELECT current_schema();'` exited 2 with `psql: error: invalid URI query parameter: "schema"`. Thus the primary preflight, both out-of-band index builds, verification, and physical rollback do not execute as written. The docker fallback at runbook `:43-45` also omits the schema/search path and can silently operate on `public` instead. **Remedy:** construct and verify a credential-safe, libpq-compatible `PSQL_URL` with the Prisma-only `schema` parameter removed, separately validate the intended schema, and set a safely quoted `search_path` for every psql call (or schema-qualify every object). Put `PGOPTIONS` with the bounded lock timeout on the two authoritative `CREATE INDEX CONCURRENTLY` commands, not only on the illustrative ellipsis at `:66-70`, and rehearse the exact commands for both public and non-public schema URLs.

3. **[must-fix | Feasibility + rollback — both `migrate resolve --applied` recovery paths fail from the documented working directory, and the dev path can target the wrong database.]** The manual-ALTER fallback tells the root-shell operator to run `npx prisma migrate resolve --applied ...` at `docs/runbooks/batch-4-rollback.md:144-147`; the checksum repair deletes the dev history row and then runs the same command at `:321-325`. The repository-root `package.json` has no Prisma schema location, while the schema is under `packages/db/prisma/`. Running the exact resolve command against the fixture URL exited 1 before connecting: `Could not find Prisma Schema ... schema.prisma: file not found; prisma/schema.prisma: file not found`. In the dev sequence this happens *after* the history row was deleted, so the database objects remain but the next deploy sees the migration pending and its `ADD COLUMN` fails. Moreover, line 322 targets `DEV_DATABASE_URL`, but line 324 does not export that URL to Prisma; adding only `--schema` could resolve the migration against the default database instead of the dev database. **Remedy:** use and rehearse `DATABASE_URL="$DEV_DATABASE_URL" npx prisma migrate resolve --schema packages/db/prisma/schema.prisma --applied 20260816165548_batch4_session_usage` for the dev path, add the same explicit `--schema` to the production fallback, and verify the resulting history row on that same target before continuing.

4. **[must-fix | Scope + production-window readiness — `migrate deploy` applies three pending migrations, not “only the four columns,” so the runbook silently drops another batch's deploy obligations.]** The runbook claims that step 1.3 “applies only the four columns” at `docs/runbooks/batch-4-rollback.md:149-151` and its preflight checks only whether `20260816165548` is recorded at `:31-38`. But the target tree orders `20260816180000_task_status_backlog` and `20260816180100_tasks_visibility` immediately after it; the latter performs two table alters, creates `TriggerFire`, creates four ordinary indexes, and adds two foreign keys at `packages/db/prisma/migrations/20260816180100_tasks_visibility/migration.sql:1-54`. Starting a fixture schema with the nine migrations before batch 4, `prisma migrate status` reported exactly these three pending migrations, and the runbook's current deploy command applied all three in order. Batch 2.5's own authoritative deploy order requires `db:backfill-task-source` before restart (`docs/runbooks/batch-2.5-rollback.md:7-20`) and separately calls out the blocking Task indexes at `:37-58`; neither obligation appears here. **Remedy:** make preflight run `prisma migrate status --schema packages/db/prisma/schema.prisma` and assert the complete pending set. If the two batch-2.5 migrations are pending—as the repository ordering implies—compose their runbook into this production window, including lock handling and `db:backfill-task-source`, or establish and record evidence that they are already applied. Do not claim or proceed on the single-migration path until the history proves it.

## should-fix

1. **[should-fix | Coherence — the runbook rejects a URL-construction method that works on the pinned Prisma version.]** `docs/runbooks/batch-4-rollback.md:133-136` says `URLSearchParams.set` serializes a space as `+` and Prisma will pass that literal plus to PostgreSQL. A Prisma 6.19 fixture connection using `options=-c+lock_timeout%3D3s` returned `SHOW lock_timeout = 3s`; the claim is false. The current hand-built `%20` form also discards any pre-existing `options` semantics when a second options key is appended (the pinned driver used the last value in a probe). This does not break the documented common `schema`-only case, so it is survivable. **Remedy:** use `URL`/`URLSearchParams.set` to replace the option deterministically, preserve unrelated query parameters, and retain the no-credential-output parameter check.

2. **[should-fix | Scope + evidence coherence — the binding retrospective artifact is still missing, so complete disposition cannot be independently confirmed.]** The fixes spec says the authoritative retrospective file is absent and relies on the brief as a faithful transcript at `docs/specs/batch-4-fixes-usage-correctness.md:16-21`; assumption A1 explicitly admits that an extra finding or caveat would be missed at `:723-728`. The revised plan carries the same unresolved gap at `docs/plans/batch-4-fixes-usage-correctness-plan.md:1484-1499`. The brief does enumerate 3 must-fix and 1 should-fix at `docs/briefs/batch-4-fixes-usage-correctness.md:5-74`, and all four enumerated items are dispositioned, so there is no concrete product failure to elevate this finding. **Remedy:** persist the original retrospective output (or a content-addressed exact export) in `docs/reviews/` and add a one-to-one disposition table; do not rely only on a later document's claim that it transcribed the source verbatim.

3. **[should-fix | Coherence + defensive feasibility — `extractUsage` is not total over `unknown` as its contract states.]** The exported function promises “any payload ... yields `{}` and nothing throws” at `packages/db/src/usage.ts:115-118`, but invalid fields are rendered with raw `JSON.stringify` at `:28-32` before `tokenCount` returns at `:40-44`. Reprobes with `{usage:{input_tokens:1n}}` and a cyclic invalid value both threw `TypeError` (`Do not know how to serialize a BigInt`; `Converting circular structure to JSON`). Stored Prisma JSON cannot contain those values, so the live ingest path is not presently exposed and this remains survivable. **Remedy:** make diagnostic rendering non-throwing (guard `JSON.stringify`, handle bigint/cycles with a bounded fallback) and add totality cases that exercise present-but-invalid non-JSON values, not only valid JSON scalars.
