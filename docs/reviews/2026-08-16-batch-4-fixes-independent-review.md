Verdict: **FAIL — 4 must-fix, 2 should-fix.** Reviewed `aaad446bdf75a2bdcf7d518c10e823bf6840670c..55f092920f2ac4f972270d07b9328de3f9bb5d79` through feasibility, scope, coherence, and a focused feasibility pass on advisory-lock deletion, migration failure/recovery, rollback/redeploy, and exact aggregation. `npm install`, `npm run build -w @agentos/web`, `npm test` (348 pass, 1 intentional skip), and `npm run typecheck` passed in the required order. A fixture-only `npm run test:db` did not pass (107 pass, 2 fail); the Batch4 failure and the unrelated webhook failure are distinguished below. The ten-file implementation diff stays within the revised plan: no `apps/web`, `schema.prisma`, new migration folder, production operation, service restart, or credential was introduced.

## must-fix

1. **[MF-1 — Feasibility + coherence; focused feasibility pass] The documented `55P03` recovery cannot be executed by retrying as instructed because Prisma first leaves a failed migration record.** Location: the runbook runs `migrate deploy` at `docs/runbooks/batch-4-rollback.md:116-131`, then says that on `55P03` nothing was applied and the operator should retry at `docs/runbooks/batch-4-rollback.md:144-147`; it mentions `--rolled-back` only indirectly at `docs/runbooks/batch-4-rollback.md:277-281`. Reproduction on fixture-built schema `batch4_partial_cmswp9`: an `ACCESS EXCLUSIVE` holder forced the migration's bounded wait to fail with `P3018`, database code `55P03`; queries then found 0 Batch4 columns and 0 Batch4 indexes, but `_prisma_migrations` contained an unfinished failed row. After releasing the lock, rerunning the exact deploy command failed before SQL with `P3009: migrate found failed migrations ... new migrations will not be applied`. Running `npx prisma migrate resolve --rolled-back 20260816165548_batch4_session_usage` then succeeded. In the stated production scenario, an ordinary writer delays the `ACCESS EXCLUSIVE` `ALTER TABLE`, the 3-second bound fires, and the operator's documented retry is permanently blocked even though PostgreSQL rolled the objects back. **Remedy:** make the `55P03` branch explicit: inspect the four columns/two indexes and the migration row, require the verified all-absent state, run `migrate resolve --rolled-back 20260816165548_batch4_session_usage`, verify the row is marked rolled back, and only then retry; give a separate stop-and-reconcile path if any object survived.

2. **[MF-2 — Feasibility; test evidence + focused feasibility pass] `usage.dbtest` test 2b races its own lock holder, so the required real-Postgres gate is red and can report a product failure when no contention occurred.** Location: test 2b starts the holding transaction at `packages/api/src/usage.dbtest.ts:264-267` but starts the contender at `packages/api/src/usage.dbtest.ts:269-270` without awaiting a signal after the holder's lock query. The mandatory full `npm run test:db` run on Node `v26.5.0` failed test 2b after 470 ms because the contender returned `"resolved"`; running `usage.dbtest.ts` alone passed and observed Prisma `P2010` wrapping PostgreSQL `55P03` after 3218 ms. That schedule dependence is the missing barrier, not a usage-lock product defect. The same full run also returned one unrelated `503` in `hooks.dbtest.ts:97`; that file is outside this diff and is not evidence against Batch4. Finally, in an isolated worktree/schema, deleting only `packages/db/src/usage.ts:313` made exactly test 1 fail with `stored 10` instead of `30` and test 2b fail with `resolved`, while tests 0/2a/3/4 passed—so the product lock and deletion sensitivity are independently established. **Remedy:** resolve a `holderLocked` promise immediately after `tx.$queryRaw` acquires the advisory lock, await it before starting the contender, release in `finally`, and rerun the full DB suite on Node 26 until the gate is deterministic.

3. **[MF-3 — Feasibility + coherence; usage/cost precision] Cost aggregation silently rounds an exact half-unit down because it sums binary `number`s before constructing `Prisma.Decimal`.** Location: `SessionUsage.costUsd` is a `number` at `packages/db/src/usage.ts:9-14`; `sumUsage` adds those numbers at `packages/db/src/usage.ts:174-181`; only afterwards does `costColumn` construct and round a decimal at `packages/db/src/usage.ts:207-221`, despite the comment at `packages/db/src/usage.ts:55-56` calling the accumulated values exact. Reproduction against the built module: two valid terminal payloads with `total_cost_usd` `0.000001` and `0.000049` produced JS sum `0.000049999999999999996` and stored `"0"`; `new Prisma.Decimal("0.000001").plus("0.000049").toDecimalPlaces(4)` correctly produced `"0.0001"`. This is a plausible, silent wrong monetary value, and summing stored session costs can accumulate the per-session error. **Remedy:** convert each accepted cost to `Prisma.Decimal` before aggregation, add with decimal arithmetic, round once at the column boundary, and add this exact half-unit regression alongside the existing range/overflow tests.

4. **[MF-4 — Scope + coherence] The branch omits two artifacts this review requires, so it cannot prove that every authoritative prior finding was dispositioned.** Location: the brief declares `docs/reviews/2026-08-16-batch-4-sol-retro-review.md` the binding, verbatim scope source at `docs/briefs/batch-4-fixes-usage-correctness.md:7-15`; the spec admits that file is unavailable and that a fourth finding or caveat could therefore be missed at `docs/specs/batch-4-fixes-usage-correctness.md:723-728`. `git ls-tree -r --name-only 55f0929 | rg 'batch-4.*(retro|implementation.*output)|implementation-output|sol-retro'` returned no paths: neither the retrospective review nor the requested implementation output is on the reviewed branch. The brief's three must-fix/one should-fix and the committed plan review's six must-fix/two should-fix are visibly implemented, but that is not evidence that the missing source contained nothing else. **Remedy:** persist the exact retrospective task output and implementation output (or a canonical, content-hashed verbatim archive of both) on the branch, then add a one-to-one disposition table and rerun this scope pass.

## should-fix

1. **[SF-1 — Feasibility; backfill boundedness] The backfill is restartable but not bounded in memory.** Location: `backfillSessionUsage` materializes every matching session in one unbounded `findMany` and sets `scanned` from that array at `packages/db/src/usage.ts:358-370`; it also retains every failure message at `packages/db/src/usage.ts:371-375`, although the CLI prints only 20 at `packages/db/src/usage.ts:397-405`. The revised plan justifies this with the current 99-session population at `docs/plans/batch-4-fixes-usage-correctness-plan.md:654-657`, so it is survivable for this production window, and the real-DB repair test proved rerun convergence (`updated 2/failed 1`, then `updated 1/failed 0`). After sustained growth or a systemic per-session error, however, one run retains all ids plus all error strings before it can report; a killed run starts the full materialization and prefix scan again. **Remedy:** page by a stable unique cursor (for example `id`) with a fixed `take`, keep aggregate counts plus only the first 20 diagnostics, and retain the per-session transaction/retry semantics.

2. **[SF-2 — Coherence; runbook traceability] The runbook's version header does not identify the commit it was rehearsed against.** Location: the authoritative spec requires a header carrying the commit at `docs/specs/batch-4-fixes-usage-correctness.md:496-500`, but `docs/runbooks/batch-4-rollback.md:3-9` says only "this batch's branch". The independently repeated fixture rehearsal did validate concurrent index create/drop, the four-column physical rollback with `costUsd` preserved, migration-history deletion, query-bearing URL construction, forward redeploy, and `db:drift-check` exit 0; the problem is auditability, not the successful commands. **Remedy:** pin the exact implementation/rehearsal commit (currently `55f092920f2ac4f972270d07b9328de3f9bb5d79`, or the eventual repaired head) in the header and update it whenever the commands or migration text change.
