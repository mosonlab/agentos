## Must-fix

Verdict: **FAIL — 7 must-fix, 2 should-fix.**

1. **MF-1 — [Feasibility, risk-focused feasibility] Goal claim is not assigned the mandatory Goal → Task → Run lock protocol, so pause/cancel can race a claim through the reverse Run → Task write order.** The plan declares that all Goal mutators use the global lock order (`docs/plans/goal-5a0-idempotent-execution-kernel-plan.md:15`, `:136`) but the claim step only adds eligibility predicates and leaves the claim CAS as winner (`:216-217`). The current claim transaction reads candidates, updates the Run, then updates the Task (`packages/api/src/app.ts:2558-2603`, `:2642-2644`); it does not lock Goal or Task first. This can deadlock with cancel, and a join predicate alone does not implement the spec's lock-order invariant (`docs/specs/goal-5a0-single-flight-lineage-safety-kernel.md:217-221`). Revise Step 8 to specify the exact Goal-linked claim transaction/lock order, bounded deadlock/serialization handling, and a cancel-versus-claim controlled-interleaving test in addition to pause-versus-claim. Evidence command: `nl -ba packages/api/src/app.ts | sed -n '2597,2605p;2640,2645p'`. Output: line 2601 is `tx.run.updateMany`; line 2643 is the later `tx.task.update`; no Goal/Task lock appears.

2. **MF-2 — [Feasibility, migration safety] The migration's “built-in PostgreSQL SHA-256” backfill is not executable on the declared repository baseline.** Step 2 requires `sha256('migration:' || Task.id)` and calls it built-in (`docs/plans/goal-5a0-idempotent-execution-kernel-plan.md:97`; spec source at `docs/specs/goal-5a0-single-flight-lineage-safety-kernel.md:842-847`), but the repository declares PostgreSQL 16 (`docker-compose.yml:2-4`) and contains no `pgcrypto` extension or SQL digest helper. Revise the plan to name an executable mechanism—normally `CREATE EXTENSION ... pgcrypto` plus `encode(digest(..., 'sha256'), 'hex')`, with privilege/preflight/rollback handling, or a deterministic pre-migration application backfill—and add a rehearsal assertion for the exact 64-hex value. Evidence command: `rg -n 'CREATE EXTENSION|pgcrypto|digest\(|sha256' packages/db/prisma packages/db/scripts deploy`. Output: only `deploy/rehearse-postgres-backup-restore.sh:421: archive-sha256` matches; no database helper or extension does.

3. **MF-3 — [Coherence, migration safety] Mixed null/non-null historical Run lineage is silently converted into invented lineage instead of failing closed.** Preflight rejects only more than one *distinct non-null* `Run.goalId` (`docs/specs/goal-5a0-single-flight-lineage-safety-kernel.md:823-834`), while backfill treats one non-null ID as sufficient and copies the tuple to *every* Run (`:842-847`; plan `docs/plans/goal-5a0-idempotent-execution-kernel-plan.md:97`, `:115-121`). A Task with one null Run and one Goal-linked Run passes that rule, then has its null Run relabelled. That contradicts the “only unambiguous history” promise and A8. Add a fail-closed mixed-null fixture/rule (and Session-vs-Run Goal identity checks), or define and evidence an authoritative inference rule that makes the rewrite legitimate. Evidence command: `rg -ni 'distinct non-null|exactly one non-null|copy the tuple to every Run|mixed' docs/specs/goal-5a0-single-flight-lineage-safety-kernel.md docs/plans/goal-5a0-idempotent-execution-kernel-plan.md`. Output: spec lines 827, 842, and 847 contain the permissive rules; there is no `mixed` match.

4. **MF-4 — [Coherence] Restart event cardinality is self-contradictory, so the atomicity tests have no single executable oracle.** The shared-kernel step says every successful non-no-op transition writes exactly one event (`docs/plans/goal-5a0-idempotent-execution-kernel-plan.md:139`), while restart requires both `GOAL_RESTARTED` and `DISPATCH_CREATED` in the same transaction (`:219`; spec `docs/specs/goal-5a0-single-flight-lineage-safety-kernel.md:565-577`). Choose and state one model—either restart is one operation containing two named durable events, or it has one compound event—and update event-count assertions, observability wording, and the evidence packet accordingly. Evidence command: `nl -ba docs/plans/goal-5a0-idempotent-execution-kernel-plan.md | sed -n '132,142p;214,222p'`. Output: line 139 says “exactly one durable event”; line 219 says “GOAL_RESTARTED plus DISPATCH_CREATED”.

5. **MF-5 — [Feasibility, evidence] `Goal.spendUsd` cannot remain the promised known-provider subtotal because the plan does not define how it is synchronized with Session usage.** Step 10 says to preserve `Goal.spendUsd` as the subtotal (`docs/plans/goal-5a0-idempotent-execution-kernel-plan.md:247-265`), but its file list omits the usage aggregation module, and the current recompute path only updates `Session` (`packages/db/src/usage.ts:326-349`). The stored Goal column currently defaults to zero (`packages/db/prisma/schema.prisma:574-583`) and no usage path updates it. Specify whether Goal responses derive and override the compatibility field transactionally from terminal Sessions or whether usage ingestion maintains it; name the files, rounding/overflow rule, concurrency strategy, and real-DB tests proving `spendUsd == knownProviderCostUsd` under late and concurrent FINAL_OUTPUT recomputes. Evidence command: `rg -n 'Goal\.spendUsd|goal\.update\(|spendUsd' packages/db/src/usage.ts packages/api/src/usage.dbtest.ts packages/api/src/usage.test.ts packages/api/src/app.ts packages/db/prisma/schema.prisma`. Output: schema line 582 is the only `spendUsd` hit; all `goal.update` hits are in `app.ts`, and none writes spend.

6. **MF-6 — [Scope] The plan does not fence inherited Inbox writes on Goal-linked failure paths, despite Inbox integration and Goal lifecycle notifications being explicitly out of scope.** The current completion path creates an Inbox message when the run budget is exhausted (`packages/api/src/app.ts:3236-3245`) and may create another on authentication-circuit failure (`:3248-3268`). Step 6 replaces Goal completion semantics but never dispositions these side effects, while Step 9 broadly says to preserve Inbox waiting behavior and “do not add Inbox integration” (`docs/plans/goal-5a0-idempotent-execution-kernel-plan.md:175-183`, `:238-245`). State exactly which existing system-wide messages remain applicable and explicitly suppress/defer Goal lifecycle/budget notifications assigned to Goal 5a1; add negative tests proving Goal 5a0 creates no forbidden Inbox record. Evidence command: `rg -n 'inboxMessage\.create' packages/api/src/app.ts packages/api/src/reconcile.ts packages/db/src/workflow.ts`. Output: completion contains writers at `app.ts:3237` and `app.ts:3259` that the plan does not disposition.

7. **MF-7 — [Feasibility, rollback] The destructive rollback instruction “drop ... enum values” is not an exact executable PostgreSQL 16 procedure.** The plan requires dependency-ordered removal and old-client compatibility (`docs/plans/goal-5a0-idempotent-execution-kernel-plan.md:321-355`), but PostgreSQL has no direct `DROP VALUE`; removing `failed`/`cancelled` requires rebuilding/casting the enum type after translating rows and handling defaults/dependencies. The final plan must name the temporary type, casts/default removal and restoration, dependent objects, transaction/abort points, validation queries, and old-client rehearsal—or explicitly make schema-retaining roll-forward-off the only supported rollback. Evidence command: `rg -n 'ALTER TYPE|DROP TYPE|CREATE TYPE' packages/db/prisma/migrations`. Output: the repository uses enum `CREATE TYPE`, `ALTER TYPE ... ADD VALUE`, and one whole-type `DROP TYPE`; it contains no value-removal sequence. `docker-compose.yml:3` fixes the target at PostgreSQL 16.

## Should-fix

1. **SF-1 — [Coherence] Predecessor and event identity are only ID-linked, so the database/verifier can accept a cross-Goal or cross-generation audit chain.** The predecessor FK points only to `Task.id`, and its check only distinguishes iteration 1 from iteration >1 (`docs/specs/goal-5a0-single-flight-lineage-safety-kernel.md:303-320`). `GoalExecutionEvent` independently references Goal, Task, and Run IDs without a tuple-consistency constraint (`:359-383`), while the verifier list omits predecessor/event mismatches (`docs/plans/goal-5a0-idempotent-execution-kernel-plan.md:115-117`). Add composite constraints where practical or, at minimum, invariant queries plus direct-corruption tests proving predecessor continuity and event Goal/generation/iteration consistency. Evidence command: `rg -n 'goalPredecessorTaskId|GoalExecutionEvent|event.*mismatch|predecessor' docs/specs/goal-5a0-single-flight-lineage-safety-kernel.md docs/plans/goal-5a0-idempotent-execution-kernel-plan.md`. Output: spec lines 305 and 320 show the ID-only FK/shape check; no event/predecessor mismatch verifier is returned.

2. **SF-2 — [Feasibility, observability] “Aggregation-stable structured records” does not yet name a queryable logging contract.** Step 10 lists metric names but not the logger, record discriminator/version, numeric value field, sink/retention assumption, or executable aggregation queries (`docs/plans/goal-5a0-idempotent-execution-kernel-plan.md:247-265`). This is survivable because raw JSON logs can carry the data, but it weakens the acceptance claim that signals are queryable. Define the exact JSON schema and evidence queries (including gauge semantics) without adding Goal 5a1 alerts. Evidence command: `rg -n 'prom-client|opentelemetry|metrics|pino|winston' package.json packages/*/package.json`. Output: no matches (`rg` exit 1), matching the plan's stated baseline.
