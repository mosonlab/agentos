The canonical `pr-engineer-workflow` hands a human a clean, fast-forward pull request whose body accurately records the goal, delivered change, verification, reviews, and Anneal identities at the post-review head.

Route: implementation=senior-dev-luna - the change is a template-scoped prompt and runner delivery transition with exact command, tree, body, and failure-path assertions.

Background: Word-factory PR #1 exposed two defects in the four-step `pr-engineer-workflow`. First, the pull-request tree contained `.chain/fix/word-factory-pytest-green/spec.md`. `specificationMaterializationForDirectImplementation` in `packages/api/src/specification-fidelity.ts` materializes the brief at `specificationPathForBranch(branch)`, and `prepareSpecificationVerification` reads it at the implementation head before either review is claimed. It must survive both reviews. Direct and compound workflows later call `createSanitizedTree` in `packages/merge-executor/src/github.ts`, but this workflow has no merge step. Second, `deliverWorkspace` in `packages/runner/src/delivery.ts` creates the PR with `--body "Automated delivery for Anneal task <id>."`; when a later step finds the open PR, `openPullRequest` returns immediately, so the final step never edits the body.

The claim already gives the runner `task.description`, `task.chainId`, template-step identity, and only the `priorOutputs` declared for that step. Thus step 4 holds the two review bodies but not the implementation output, while `readSessionTaskOutputStatus` in `packages/runner/src/api.ts` currently exposes the current output's identity without its body. Those facts are insufficient for a truthful final body. The clean-tree responsibility belongs in the final step prompt, with delivery enforcing its result: a runner-created commit after `task_output` would make the output JSON and `TaskStepOutput.commitSha` name the pre-cleanup head while completion names the delivered head, which `canonicalOutputRefusal` in `packages/api/src/canonical-task-output.ts` correctly refuses. A final-step cleanup commit preserves every earlier commit, leaves the spec readable at the pinned implementation head, and advances the shared branch by ordinary fast-forward push.

Changes:
1. Change only the canonical `pr-engineer-workflow` final prompt in `agents/templates/pr-engineer-workflow/04-apply-review-fixes.md` so `Apply review fixes` removes the complete tracked `.chain/` directory after using the review evidence, commits that deletion together with any adopted fixes on top of the reviewed history, and persists `fixed-implementation` only after the cleanup commit so its `headSha` is the human-mergeable head. A failed removal, failed commit, or remaining tracked `.chain/` entry is an error. A retry after a successful push and failed PR edit may start at the already-clean cleanup commit; it preserves that head rather than recreating bookkeeping or inventing another commit. Keep the two reviews' access unchanged: `.chain/<branchName>/spec.md` remains present at the implementation head and both review roles continue reading that pinned commit. Do not change the four step indexes, layers, Agents, output kinds, `priorOutputKinds`, `opensPullRequest`, `requiresCommit`, or `baseFromStepIndex`.
2. Give PR-workflow delivery a run-bound structured evidence handoff. Extend the session status projection in `packages/api/src/routes/session.ts` and its validated `SessionTaskOutputStatus` mirror in `packages/runner/src/api.ts` so only the canonical PR implementation or fixed-implementation Run holds persisted output bodies through its chain index. Entries contain Task id, chain index, kind, body, and commit SHA, constrained to the claimed `projectId` and `chainId`; `runner.ts` passes them to `deliverWorkspace`. Do not widen prompt `priorOutputs`, expose sibling evidence to a blind review, or derive text from provider output, activity prose, or repository contents.
3. In `packages/runner/src/delivery.ts`, identify the exact current canonical template and the `implementation` or `fixed-implementation` output kind rather than inferring behavior from task names or `opensPullRequest`. Before publishing the final fixed step, verify that its current `HEAD` contains no tracked `.chain/` content; refusal occurs before `git push`. Keep the existing non-force `git push --set-upstream origin <branch>` path and never create, amend, reset, rebase, or force-push a cleanup commit in delivery.
4. Replace the placeholder only for `pr-engineer-workflow` with one deterministic Markdown body containing sections `Goal`, `Summary`, `Verification`, `Review outcomes`, and `Anneal`. `Goal` is exactly the first line of `task.description`. `Summary` uses the implementation output's `summary`; after review it also lists each adopted fix from `closedFindings.codeEvidence`, or states that no review-driven code change was required when the adopted set is empty. `Verification` renders the implementation and fixed-step `testsRun` entries verbatim; update both PR-workflow step prompts so each entry records the exact command and its observed exit/result summary. A present empty list says `No commands reported in the task output.` and a section unavailable because the workflow has not reached it says exactly `Not available at this step.`; delivery never invents `PASS`. `Review outcomes` initially uses that unavailable marker; the final body reports each Sol and blind finding with its existing id, severity, and title, then its final disposition and reason, closed evidence where applicable, and the fixed output's `residualRisks`. `Anneal` contains the current Task id and non-null chain id.
5. Use that body in `gh pr create --body` for step 1. If step 1 finds an already-open PR on the shared head, edit it to the same initial body instead of returning it unchanged. After the final step's successful push and open-PR lookup, run `gh pr edit` for that PR with the complete post-review body and read back the body exactly. A missing or malformed required canonical output, missing chain identity, absent open PR at the final step, edit failure, unreadable read-back, or body mismatch fails delivery. Publish the prompt edits through the existing canonical prompt-transition mechanism so existing Projects receive the new prompt generation while already-instantiated chains retain their pinned generation; change no canonical-sync behavior or template structure.
6. Update `docs/operator-api.md` with the canonical PR workflow's clean-tree and five-section body handover and the machine-only, run-bound evidence projection. Add assertions to `scripts/operator-api-docs.test.mjs`; do not edit `docs/runbooks/add-a-project.md`.

Out of scope: direct or compound templates and their merge steps; `createSanitizedTree` or any other merge-executor sanitizing behavior; `specificationPathForBranch`, specification materialization or fidelity semantics; the review roles' reading of `.chain/<branch>/spec.md`; repository routes, Project bootstrap, canonical-sync machinery, dependency provisioning, regression tooling, or other chain B/A2/C/D1 work; `docs/runbooks/add-a-project.md`; any web change; database schema changes; any rebase, reset, amend, force-push, or other history rewrite.

Constraints: Both behaviors are exact-name scoped to the current canonical `pr-engineer-workflow`; custom templates and retired, direct, and compound workflows retain their delivery behavior. The implementation head remains immutable and reviewable with its spec. The final Task output, completion head, pushed head, and PR head all name the same cleanup commit. Structured JSON is parsed against the existing canonical output contracts in `packages/db/src/canonical-output-schema.ts`; unavailable markers describe only a real sequencing gap. Cleanup and PR-body writes fail loudly: a step cannot succeed after leaving `.chain/` in the final tree or an old, unverifiable, or partially updated body on the PR.

Acceptance:
1. `npm run lint` and `scripts/merge-gate.sh --expect-head <exact commit>` are green.
2. `packages/db/src/template-sources.test.ts` proves the PR workflow remains exactly four steps with the existing structural frontmatter, its implementation prompt requires command plus observed result in every `testsRun` entry, and only its final prompt requires a committed `.chain/` removal before `fixed-implementation`. Prompt assertions for `agents/roles/review-coordinator-sol.md` and `agents/roles/review-coordinator-opus.md`, plus `packages/api/src/pr-engineer-workflow.dbtest.ts`, prove both reviews still read `.chain/<branch>/spec.md` at `IMPLEMENTATION_HEAD`. Canonical prompt-sync tests prove the prompt-only transition installs on existing Projects without mutating instantiated chains or changing the graph.
3. Runner tests using a real temporary Git repository start the final step from a commit containing `.chain/<branch>/spec.md`, add a descendant cleanup commit, and prove delivery pushes that exact descendant without any force/history-rewrite command and with no `.chain/` entry in `HEAD`. The same test family proves retained `.chain/` or a cleanup/output/completion head mismatch fails before push or `gh` invocation, while a retry starting at the already-pushed clean head creates no new commit and can complete the pending PR edit.
4. Session-route and runner API tests prove the implementation delivery receives only its current `implementation` body, final delivery receives exactly `implementation`, `sol-findings`, `blind-findings`, and `fixed-implementation` from the same Project and chain in order, and malformed, foreign-chain, or missing required evidence is rejected rather than omitted or guessed.
5. `packages/runner/src/delivery.test.ts` asserts the exact initial body and `gh pr create`/existing-PR edit arguments, including goal, implementation summary, reported command/result strings, unavailable review marker, Task id, and chain id. It asserts the exact final `gh pr edit` body, including both review reports, every disposition, adopted-fix evidence, fixed-step verification, residual risks, and updated Task id; an empty reported-command list receives the specified factual marker.
6. Delivery tests prove final edit failure, unreadable or unequal read-back, missing final PR, and malformed output return `pushStatus: "FAILED"` with the responsible operation. Separate fixtures prove direct, compound, custom, non-GitHub, and ordinary no-PR steps keep existing behavior. `npm run test:operator-api-docs` asserts the handbook's clean-tree, body-section, evidence-source, and machine-authentication claims.
