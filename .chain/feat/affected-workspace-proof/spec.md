Every model-executed Anneal Run verifies only the workspace or workspaces affected by its change, while the Regression step and gate worker own whole-repository proof.

Route: implementation=senior-dev - the prompt-assembly contract spans three adapters plus canonical instruction text, and the unit suite cannot witness its effect on what models actually run.

Background: This brief is based on `origin/main` commit `75ea468446df55fd60c564bcae1f38f13dc0f782`. From 2026-09-01 10:00-10:45Z, five concurrent production chains on a 10-core macOS host repeatedly ran root-wide `tsc -p tsconfig.json` (up to three processes per Run), `eslint .`, and workspace test suites. The one-minute load average reached 88-90; heartbeat-starved Runs became LOST, one Run hit the tool-inactivity stall timeout, and API requests timed out. Review Runs did the same, including Pi/Sol Run `cmtihk6fc07jxmpm63jpmbp92` and Claude/Opus Run `cmtihk6hc07k1mpm6s7dpgwbt`. L1 separately makes runner claims load-aware; L2 must be independently sufficient and must not touch the claim or poll loop.

The repository makes an innocent root command expensive: root `build` serially builds seven workspaces, `typecheck` and `test` select all workspaces, and `lint` reaches both `biome lint` and `eslint .` (`package.json:14-20`). Yet the only platform pin is in `codexPromptSections`: it returns no section unless `codexNativeSubagentProfile(claim.run, "CODEX")` returns the native Luna profile, then appends the two Implementation-proof lines after the subagent policy (`packages/runner/src/adapters/codex.ts:36-50,79-93`). Claude and Pi only reject an invalid native-subagent snapshot and otherwise return no prompt section (`packages/runner/src/adapters/claude.ts:170-175`; `packages/runner/src/adapters/pi.ts:322-327`).

The common assembly point is `buildPrompt`, which constructs every provider prompt and inserts provider `promptSections` at one seam (`packages/runner/src/adapters.ts:52-131`, especially line 78). Fresh execution passes that prompt to the selected adapter, while `inputForRunner` currently selects only the continuation input for a resume (`packages/runner/src/runner.ts:337-345,391-395,431-441`; `packages/runner/src/adapters/runtime.ts:244-250`). The runner clones or checks out the repository into `workspace.path`, and `spawnAdapterRuntime` launches each provider with that directory as `cwd`, so tracked `AGENTS.md` is available to Claude, Codex, and Pi as repository text (`packages/runner/src/workspace.ts:341-372,394-408`; `packages/runner/src/adapters/runtime.ts:278-295`). That repository-visible rule complements, but does not replace, the platform-owned prompt pin.

Changes:
1. Establish one shared, platform-pinned proof-boundary paragraph in the runner's common session-input assembly and deliver it byte-identically to fresh and resumed Claude, Codex, and Pi invocations. Do not filter by provider or output kind: it covers the model-executed `spec`, `plan`, `plan-review`, `revised-plan`, `revalidation`, `implementation`, `sol-findings`, `blind-findings`, `fixed-implementation`, `documentation`, and `regression-verification-v2` rows identified by the canonical contracts (`packages/db/prisma/agent-contract.test.ts:172-190,248-260`). The paragraph must say that verification inside a Run is limited to affected workspaces: `npm run <script> -w <workspace>` or the workspace-local equivalent, `eslint`/`tsc` over changed paths or one affected workspace, and focused test files. It must forbid root `npm run build`, `npm run lint`, `npm run typecheck`, `npm run test`, `eslint .`, and the repository Merge Gate inside a Run. It must assign repository-wide proof to the Regression step and gate worker, and require review steps to verify claims by reading plus workspace-scoped checks only. A resume must receive this boundary with its continuation rather than relying only on provider conversation memory. The paragraph opens with the heading `Platform-pinned Implementation proof boundary` so that the existing template and role references to "the platform-pinned Implementation proof boundary" (`agents/templates/direct-engineer-workflow/02-implementation.md:14`, `agents/templates/compound-engineer-workflow/05-implementation.md:14`, `agents/roles/implementation-plan-executioner.md:25`) resolve to it by name.
2. Remove the two proof-boundary lines from `codexPromptSections` while retaining its native-subagent heading, pinned model, effort, concurrency, collaboration-tool, fresh/resume snapshot, and no-escalation lines (`packages/runner/src/adapters/codex.ts:82-92`). The Codex section remains subagent-specific; the shared section becomes the rule's only runner source.
3. State the same Run-only rule exactly once in `AGENTS.md` under `Platform runs`. That section has the precise “agent executing inside an Anneal run” trigger and already assigns gates to the mechanical tail (`AGENTS.md:64-72`); `Test safely` also governs host-side local pre-gate work and explicitly permits exact local or dispatched gates, so placing the prohibition there would blur two contexts (`AGENTS.md:26-52`). Preserve its existing focused-test rule. Do not duplicate the paragraph in `CONTRIBUTING.md`; its fresh-worktree `npm run build -w @anneal/db` bootstrap remains valid outside a Run (`CONTRIBUTING.md:153-158`).
4. Align the one canonical prompt that explicitly contradicts the boundary: remove the legacy post-fix instruction to “run the repository's required exact-head gate” from `agents/roles/review-coordinator-sol.md:65-73` and require the platform Regression handoff instead. Grep found no direct or compound template that instructs a root-wide build, lint, typecheck, or test; their Regression prompts only invoke the existing platform-owned `scripts/regression-verification.sh` prepare/finalize flow (`agents/templates/direct-engineer-workflow/06-regression-verification.md:14-36`; `agents/templates/compound-engineer-workflow/10-regression-verification.md:14-36`). Keep the existing boundary references in both implementation templates and `agents/roles/implementation-plan-executioner.md:19-27`.

Out of scope: build caches or shared `tsbuildinfo`; replacing `tsc` or `eslint`; changing root `package.json` scripts; changes to the Merge Gate, gate worker, `scripts/regression-verification.sh`, or what the Regression step runs; runner claim or poll behavior owned by L1; dependency provisioning; web changes.

Constraints: Pin the runner paragraph in exactly one source location and state the rule exactly once in `AGENTS.md`; no provider-specific wording or fallback may drift from it. Existing canonical assertions for “the platform-pinned Implementation proof boundary” remain valid (`packages/db/prisma/agent-contract.test.ts:224,290`). Canonical role/template changes reach production through `packages/db/prisma/sync-canonical-prompts.ts`, which updates prompt and role rows (`:312-318,512-523`); no migration is required. Do not change Regression gate dispatch or weaken any existing focused-test requirement. Because `AGENTS.md` is published repository text, its edit must remain clean under the snapshot scanner.

Acceptance:
1. `packages/runner/src/adapters.test.ts` builds implementation and review claims for Claude, Codex, and Pi and proves the extracted platform paragraph occurs once and is byte-identical across providers, both for fresh start and resume. The matrix covers `implementation`, both `sol-findings`/`blind-findings` review shapes, `fixed-implementation`, and `regression-verification-v2`; start/resume stdin or prompt-hash assertions prove the actual dispatched bytes carry the paragraph.
2. Adapter tests prove the Codex native-subagent section still carries all subagent-specific lines for fresh and resume, contains neither proof-boundary sentence, and does not duplicate the shared paragraph. Claude and Pi contain no provider-local copy.
3. `packages/db/prisma/agent-contract.test.ts` remains green, including lines 224 and 290. A direct `readFileSync` plus `assert.match`/count assertion in `scripts/public-snapshot-scan.test.mjs`, following its existing repository-content contract style (`:198-211,409-440`), proves `AGENTS.md` contains one Run-scoped rule and every forbidden root command; the canonical prompt grep has only the platform-owned Regression script and no model instruction to run a repository-wide command.
4. Implementation-scoped verification is green with `npm run test -w @anneal/runner`, `npm run test -w @anneal/db`, and `npm run test:snapshot-scan`. The Regression step/gate worker records green `npm run lint` and `scripts/merge-gate.sh --expect-head <exact head>`, ending with `MERGE GATE: PASS <exact head>` for that same commit.
5. After deploy, the next comparable concurrent Runs' tool-command transcripts contain workspace selectors such as `-w @anneal/<pkg>` and focused test files, with no root `npm run build`, root `eslint .`, or other forbidden repository-wide proof. During comparable concurrent Runs on the 10-core host, the observed one-minute load average remains below 10.
