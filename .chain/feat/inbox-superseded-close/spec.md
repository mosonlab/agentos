Low-priority Inbox lifecycle follow-up. The legacy PR #44 and PR #46 task rows were directly archived through the official task archive endpoint after recording their canonical successor chains, so they no longer appear on the active board. Their historical task-linked Inbox messages remain visible because POST /inbox/messages/:messageId/close intentionally accepts only detached agent notifications and refuses task-, session-, goal-, gate-, or reply-linked messages. Design the smallest official operator-authorized, idempotent mechanism to close or supersede Inbox messages whose task has been archived as superseded legacy evidence. Preserve message bodies, decisions, replies, task outputs, runs, and activities for audit; remove only active Inbox noise. Do not mutate task status to DONE, fabricate evidence, directly write the database, or reopen/resume legacy runs.
