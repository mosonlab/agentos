A workspace-test failure on the merge gate reaches the merge tail with the failing assertion lines in its excerpt, so automatic repairs are no longer spent blind.

Route: implementation=senior-dev-luna

Background: This brief is based on `origin/main` at `d2314b91b02937b88b9af22279cfae122a3efe61`. On 2026-09-01 chain 0188ca11 failed the gate on stage `unit tests (all workspaces)` with a plain AssertionError printed in the worker log, yet the `gateFailureExcerpt` persisted into the regression verdict carried only `unit tests (all workspaces): no per-test output in gate log`, and both automatic repair attempts were spent without the assertion text (card cmtizzgrr02lgmpqfr7p992vc).

The excerpt is produced in two passes. On the worker, `forward_failure_excerpt` in `packages/runner/runtime-tools/gate-worker/run-gate.sh:372-470` reads the full gate log, keeps one 200-line ring per failing stage plus a global ring, and attributes each line to `currentStage`. It sets `currentStage` on every heading of the form `--- <name> ---` or `== <name>`, and when the heading is not one of the failing stage names it sets `currentStage` to `null` (`run-gate.sh:415-420`). The parallel unit stage replays each workspace's buffered output under a nested heading of exactly that form, `--- test: <workspace> ---` (`scripts/merge-gate.sh:776`, inside `run_workspace_script_parallel`, invoked by `parallel_unit_tests` at `:788`; the enclosing stage heading `--- unit tests (all workspaces) ---` is printed by `parallel_steps` at `scripts/gate-worker/step-engine.sh:213`). The first nested heading therefore ends attribution for the whole stage: every replayed test line lands in the global ring, the stage ring stays empty, and the forwarded excerpt contains nothing attributable. On the dispatcher, `extract_gate_failure_excerpt` in `packages/runner/runtime-tools/regression-verification.sh:108-275` then sees no failure records for the stage and emits the fallback notice. The dispatcher-side extractor keeps `currentStage` across unknown headings (`:148-152,177`), so it is not the defect.

A second, smaller loss remains once attribution is fixed: the per-stage ring keeps the last 200 lines of the stage. With eight workspaces replayed in order, a failing workspace replayed early is pushed out of the ring by the passing output that follows it.

Changes:
1. In `forward_failure_excerpt` (`run-gate.sh`), a heading that is not a failing stage name must not clear the current stage while inside a stage's replayed block; only a heading that names a stage of this gate (failing or not) or a serial `==` step heading changes the current stage. Nested per-workspace headings printed by `run_workspace_script_parallel` stay attributed to the enclosing stage. Do not change the heading format printed by `scripts/merge-gate.sh`.
2. Within each failing stage's ring, retain the failure evidence in preference to trailing passing output: lines matching the failure markers already recognised by the dispatcher extractor (`not ok`, the default reporter's failure marker, `AssertionError`, the `Error:` lines adjacent to an open failure) and their file or subtest context are kept when the 200-line cap is reached, and later passing lines are what falls out. The per-stage cap and the 65536-byte total cap stay as they are.
3. Extend `scripts/gate-worker/gate-worker.test.mjs` with a fixture log that reproduces the real layout: a `--- unit tests (all workspaces) ---` heading, then eight `--- test: <workspace> ---` blocks in which the first block contains a `not ok` and `AssertionError` and the remaining seven contain more than 200 lines of passing TAP output in total, followed by the `MERGE GATE: FAIL (unit tests (all workspaces))` verdict. Assert that the forwarded excerpt for that stage contains the assertion line and its subtest context, and that a sibling non-failing parallel stage's block is still not attributed to the failing stage.
4. Extend `packages/runner/src/regression-verification-script.test.ts` (or the existing extractor test beside it) with the forwarded output of that fixture as input, asserting the persisted excerpt carries the assertion line and no `no per-test output in gate log` notice for the failing stage.

Out of scope: The dispatcher-side extractor's own attribution logic, the excerpt byte and line budgets, the stage grouping in `scripts/merge-gate.sh` and `scripts/gate-worker/step-engine.sh`, the heading format, and the merge-tail repair budget. Do not touch `scripts/merge-gate.sh`, `scripts/run-scope-guard.sh`, `scripts/host-proof-slot.sh`, `package.json`, `CONTRIBUTING.md`, `packages/runner/scripts/build-runtime-tools.mjs`, `packages/runner/runtime-tools/regression-verification.sh`, `packages/runner/src/runner.ts`, `packages/runner/src/dependency-cache.ts`, `packages/db/**`, or `packages/api/**`; other chains own those files concurrently.

Constraints: Fail loudly with no silent fallback: an excerpt that cannot be extracted is still reported as such, never replaced by an empty string. The verdict line stays the last line of the worker output. The runtime-tools bundle inventory stays exactly six files; `run-gate.sh` is one of them and its edit ships through the ordinary build.

Acceptance:
1. `RUNNER_WORKSPACE_ROOT="$(mktemp -d)" npm run test:gate-worker` exits 0; the new fixture proves the assertion line and its context are forwarded under `unit tests (all workspaces)` and the sibling stage's lines are not.
2. `RUNNER_WORKSPACE_ROOT="$(mktemp -d)" npm run test -w @anneal/runner -- src/regression-verification-script.test.ts` exits 0 with the new end-to-end case green.
3. `npm run test:auto-deploy` exits 0 (the runtime-tools preflight and release-snapshot suites still see exactly six bundled files).
4. `npm run lint` exits 0.
5. `scripts/merge-gate.sh --expect-head <exact-implementation-oid>` exits 0 and prints `MERGE GATE: PASS <exact-implementation-oid>` for the implementation commit being delivered.
