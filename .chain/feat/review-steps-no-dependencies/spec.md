Every canonical code-review Run receives a normal checkout without materialized dependencies and still reaches its configured reviewer.

Route: implementation=senior-dev - a template contract change crosses db sync, transition registry, claim payload, and runner provisioning, and a wrong default would silently strip dependencies from implementation steps.

Background: This brief was authored against `origin/main` commit `15130554d19d438679423eba6435cdabafd75619`, fetched on 2026-09-01. Revalidation is against current `origin/main` commit `a42c30ffa338d440340c9909d599fb3e7a7a3ca3`. On the 10-core, 16 GB production Mac, seven concurrent chains drove the one-minute load average to 120; over seven days, 28% of Runs were platform-made reruns. Fresh local-mirror checkouts and APFS clonefile materialization of cached `node_modules` are cheap, but proof inside a Run is not: root `build` invokes seven workspace builds (`package.json:14`), and API `pretest:db` builds github-client, db, runner, and api before the test command (`packages/api/package.json:11-12`). Package runtime exports resolve to `dist` while types resolve to source, for example `@anneal/db` (`packages/db/package.json:8-43`), so a test in a fresh checkout currently induces cold builds. The operator's fixed ruling for this chain is narrower and mechanical: review steps judge the diff and receive no materialized dependencies at all. Test source resolution is a separate chain, and no build cache is wanted.

Canonical step structure is owned by Markdown frontmatter (`agents/README.md:9-12,24-45`). `CANONICAL_TEMPLATE_SOURCE_SPECS` defines the three workflows (`packages/db/src/template-sources.ts:15-47`), while `STRUCTURAL_FIELDS`, `TemplateStepSource`, `templateStepStructureDifferences`, and `loadTemplateStepSources` enforce the closed contract (`packages/db/src/template-sources.ts:54-126,165-203`). The actual source contains six code-review rows: direct steps 3 and 4 (`agents/templates/direct-engineer-workflow/03-code-review-sol.md:2-12`; `agents/templates/direct-engineer-workflow/04-code-review-opus-blind.md:2-12`), compound steps 6 and 7 (`agents/templates/compound-engineer-workflow/06-code-review-sol.md:2-12`; `agents/templates/compound-engineer-workflow/07-code-review-opus-blind.md:2-12`), and PR steps 2 and 3 (`agents/templates/pr-engineer-workflow/02-code-review-sol.md:2-12`; `agents/templates/pr-engineer-workflow/03-code-review-opus-blind.md:2-12`). Their bodies already require review of the integrated diff. Only the Sol role tells a canonical reviewer to run proof: the sentence beginning "Use the evidence ladder" requires narrow regressions at `agents/roles/review-coordinator-sol.md:43-44`.

`claimRun` includes the complete `TaskTemplateStep` through `candidateInclude` and returns `candidate.task` unchanged (`packages/api/src/run-claim.ts:204-225,864-900`). The runner mirrors only `name` and optional `outputKind` today in `ClaimedTask.task.templateStep` (`packages/runner/src/api.ts:55-84`). That identity flows through `GitProvenanceClaim` into `WorkspaceProvisionClaim` (`packages/runner/src/workspace.ts:105-117`). C's landed `provisionWorkspace` path now takes the Repo's `NONE | NPM_CI` policy but still unconditionally calls `materializeWorkspaceDependencies` after checkout and specification materialization (`packages/runner/src/workspace.ts:447-597`, especially line 584); `executeClaim` then performs adapter preflight and provider launch (`packages/runner/src/runner.ts:360-369,415-418`). C, chain `219a7202`, is already merged into current HEAD. All Changes below are written against C's landed shape, which is current main; the implementer must re-fetch and re-read C's merged diff before editing. A2, chain `de4443dc`, overlaps `packages/db/prisma/sync-canonical-prompts.ts`; at revalidation, `origin/feat/multi-project-a2-canonical-sync` resolves to `e3f762255955abcb0f55387c34ac4e98eb0d6ce8` while `origin/main` resolves to `a42c30ffa338d440340c9909d599fb3e7a7a3ca3`, and A2 is not merged into current main. Recheck that branch immediately before implementation; if A2 has landed, preserve its per-Project sync shape rather than restoring the current shape.

Changes:
1. Add required `TaskTemplateStep.provisionDependencies Boolean @default(true)` beside the existing step execution booleans (`packages/db/prisma/schema.prisma:504-530`) and one migration that adds the non-null column with database default `true`. Add `provisionDependencies` to the documented frontmatter in `agents/README.md:24-38` and to every canonical step file. Set it to `false` on exactly the six code-review rows named in Background and explicitly `true` on every other canonical step; do not infer the value from agent name, output kind, or layer.
2. Extend the closed Markdown contract end to end: `STRUCTURAL_FIELDS`, both source/persisted step types, parsing, and structural comparison in `packages/db/src/template-sources.ts`; and the create/upsert data in `writeCanonicalTemplate` (`packages/db/src/canonical-template-installation.ts:159-205`). Boolean parsing remains strict, and a missing frontmatter key remains an error rather than acquiring the schema default.
3. Extend `PersistedTransitionStep` and exact shape matching (`packages/db/src/canonical-template-transition.ts:506-550`) so every registered pre-R generation requires the migration-provided value `true`; an absent, false, or malformed historical field must not match by fallback. Authorize one in-place canonical adoption from `true` to `false` only for the six exact review-step identities in `adoptionDifferenceAllowed` and `currentGraphRefusal` (`packages/db/src/canonical-template-installation.ts:51-99`). Extend canonical sync's persisted projection, drift validation, explicit update, and result accounting (`packages/db/prisma/sync-canonical-prompts.ts:190-318`) so it flips those review rows across every current canonical template in every Project, including rows referenced by tasks, while refusing that transition on any other step. If A2 lands first, add the field to A2's per-Project inventory, install, validation, and reporting paths without implementing any other part of A2.
4. Carry the stored boolean on `task.templateStep` in the claim response and make it required in `ClaimedTask` (`packages/runner/src/api.ts:47-76`). Before any workspace, dependency operation, adapter preflight, or provider launch, a model runner receiving a non-null `templateStep` whose field is absent or not boolean must complete the Run non-retryably as `PROTOCOL_ERROR` with condition `template-step-provision-dependencies-missing`. A null `templateStep` remains a valid non-template claim governed only by C's Repo policy.
5. In C's landed `provisionWorkspace` path, an explicit `false` skips dependency materialization regardless of `Repo.dependencyProvisioning`; an explicit `true`, or a valid null template step, delegates unchanged to C's `NONE | NPM_CI` decision. After the review checkout exists and before adapter preflight, append the fenced Run activity `Dependency provisioning skipped: TaskTemplateStep.provisionDependencies=false`. Recording failure is not swallowed. The ordinary prompt, adapter preflight, session setup, credentials, and provider start continue on the same path; there is no dependency-free adapter variant.
6. Delete only the canonical review-round sentence at `agents/roles/review-coordinator-sol.md:43-44` that directs the reviewer to run narrow regressions. Add no replacement or runner-injected prompt text. Preserve the legacy post-fix regression paragraph at lines 65-75 because regression behavior is outside this chain.

Out of scope: any change to implementation, apply-review-fixes, or regression steps; Repo-level `NONE | NPM_CI` semantics, validation, cache behavior, or install commands owned by C; test source resolution and build removal owned by ruling 2; any build cache; root scripts, workspace scripts, or `pretest:db`; the web UI; new prompt paragraphs or runner-injected prompt text; and the Merge Gate or what it executes.

Constraints: C is a hard predecessor, and its merged contract is the authority for overlapping runner and Prisma files. The migration backfills safely with `true`; canonical sync, not a migration heuristic or template rollover, flips the six current canonical review rows to the explicit source value `false`. Historical legacy templates remain `true`. The runner has no default-by-absence: only a validated explicit `false` may suppress C's provisioning path, and no implementation, fix, regression, standalone, or malformed claim may be silently treated as dependency-free. The skip activity is durable before reviewer launch. Preserve A2 if present, preserve C's behavior for every admitted non-review path, and add no fallback from a failed contract check or activity write.

Acceptance:
1. The migration applies to an empty database and to a preceding-schema fixture, where every existing `TaskTemplateStep` reads `provisionDependencies=true`; the column is non-null with default `true`. Prisma validation and generation succeed.
2. Source-contract tests load all 24 canonical step files and prove `false` exactly at compound 6/7, direct 3/4, and PR 2/3, with `true` everywhere else; omission or a non-boolean value is rejected. `agents/README.md` documents the key.
3. Transition tests prove registered historical graphs match only with `provisionDependencies=true`. Canonical installation and sync tests prove an all-`true` current graph adopts `false` only at compound 6/7, direct 3/4, and PR 2/3, including task-referenced rows in every Project; another step changing to `false`, a missing field, or a mixed unknown shape is refused. A second sync is idempotent, and every non-review row remains `true`.
4. An API claim test proves a stored review-step value reaches `task.templateStep.provisionDependencies`; a runner contract test removes or corrupts that property and observes non-retryable `PROTOCOL_ERROR` with `template-step-provision-dependencies-missing`, zero workspace creation, zero dependency calls, zero adapter preflights, and zero provider starts. A null template step remains admitted to C's Repo decision.
5. With a C `NPM_CI` Repo and a review step set to `false`, a real workspace-provisioning plus fake-adapter test observes no `node_modules`, no dependency-cache inspection, npm, restore, or materialization call, the exact fenced skip activity before launch, and one successful adapter preflight and provider start.
6. The paired non-review fixture uses the same `NPM_CI` Repo with `provisionDependencies=true` and observes the normal C materialization result and usable `node_modules`, with no skip activity. Existing C `NONE` coverage remains unchanged.
7. `agents/roles/review-coordinator-sol.md` no longer contains "run narrow named regressions" and gains no replacement proof paragraph. `packages/db/prisma/agent-contract.test.ts`, `packages/db/src/template-sources.test.ts`, `packages/db/src/canonical-template-installation.test.ts`, and `packages/db/src/canonical-template-registry.test.ts` cover the new contract; `npm run test -w @anneal/db`, `npm run test -w @anneal/runner`, `npm run test:db -w @anneal/api -- src/blind-claim.dbtest.ts`, and `npm run lint` are green. The exact implementation commit ends with `MERGE GATE: PASS <exact-implementation-oid>` from `scripts/merge-gate.sh --expect-head <exact-implementation-oid>`.
