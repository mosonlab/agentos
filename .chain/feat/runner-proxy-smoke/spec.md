Backlog only; do not use production for evidence. Build a disposable end-to-end smoke that proves an actual Claude CLI network request traverses RUNNER_HTTP_PROXY/RUNNER_HTTPS_PROXY while host Claude settings stay excluded. Cover the default-unset path, authenticated proxy values without argv/log disclosure, RUNNER_RUN_AS_PREFIX environment preservation, and a named failure when proxy reachability is absent. Evidence must distinguish environment inheritance from observed proxy traffic and must not grant production access.
