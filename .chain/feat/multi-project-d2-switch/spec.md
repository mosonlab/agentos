Anneal's canonical full-tail workflows consume runner-shipped regression tooling, and one public onboarding contract teaches another repository to supply its sole repository-owned gate.

Route: implementation=senior-dev - a template or DEFENSE_EXACT transition that lands wrong breaks the platform's own regression and merge path for every chain, and the unit suite cannot witness a canonical template transition end to end

Background: D1 is merged at `7adfea4a`. The runner build copies exactly five canonical sources into `packages/runner/dist/runtime-tools` (`packages/runner/scripts/build-runtime-tools.mjs:7-18`, `buildRuntimeTools` at lines 111-163); each Run materializes that release-local tree outside the checkout (`packages/runner/src/workspace.ts:331-399`, `provisionAgentScratch`, `materializeRuntimeTools`); and `buildChildEnvironment` overwrites any task Secret with `AGENTOS_TOOLS: scratch.toolsDir` before every provider launch (`packages/runner/src/adapters.ts:133-174`). `COMMON_LAUNCHER_ENVIRONMENT` preserves it through a scrubbing launcher (`packages/runner/src/adapters/runtime.ts:252-266`). This corrects the pre-D1 assumption that the tools would be tracked at a second runner-package path: the landed build still takes its canonical bytes from the five `scripts/` files.

The direct and compound Regression prompts still invoke the checkout copy three times (`agents/templates/direct-engineer-workflow/06-regression-verification.md:14-38`; `agents/templates/compound-engineer-workflow/10-regression-verification.md:14-38`). Their Markdown bodies are loaded as canonical prompt source by `loadTemplateStepSources` (`packages/db/src/template-sources.ts:185-203`), so editing files alone is not a safe database cutover. Prompt-only generations are authenticated by outgoing and successor digests (`packages/db/src/canonical-template-transition.ts:49-91`, `templatePromptGenerationDigest` at lines 449-468), and installation refuses successor drift before rolling a quiescent canonical row to a per-row legacy name (`packages/db/src/canonical-template-installation.ts:120-135,230-267`). Existing chains keep the retired graph; only new chains bind the successor (`packages/db/src/canonical-template-transition.ts:489-500`).

L3's landed rule is also authoritative: an agent inside an Anneal Run never invokes a repository-wide gate; the Regression step owns that proof on the gate worker (`AGENTS.md:14-18,33-41`). One prerequisite is not yet true at briefing time: after a fresh fetch, `origin/main` is `7adfea4a`, while A2 head `16b668e3` exists only on `origin/feat/multi-project-a2-canonical-sync`. Do not instantiate D2 until that commit is an ancestor of the then-current `origin/main`; A2 line references below describe that reviewed head.

Changes:
1. Switch both canonical Regression prompt sources to resolve all three actions through the runner contract. The commands are `"${AGENTOS_TOOLS:?AGENTOS_TOOLS is required}/regression-verification.sh" prepare`, the same prefix followed by `review-fail '<concise finding IDs or defect>'`, and the same prefix followed by `finalize`. There is no checkout fallback. Register one new prompt-only legacy generation for each of `direct-engineer-workflow` and `compound-engineer-workflow`, preserving their current shapes and pinning both the outgoing and successor prompt-generation digests. Extend the transition and canonical-sync tests to prove the registered rollover occurs once, a second sync is idempotent, unregistered source drift is refused, old instantiated chains retain the old prompt, and newly instantiated chains receive the `AGENTOS_TOOLS` prompt. `sync-canonical-prompts` already loads sources, applies canonical installation, and refuses in-use prompt edits rather than mutating them (`packages/db/prisma/sync-canonical-prompts.ts:11-14,185-189,312-318`); use that existing transition, not seed-time mutation or an in-place update.
2. Keep `DEFENSE_EXACT` accurate for the new delivery contract without attempting R8's project-scoping fix. The current list has only one exact entry pointing at one of the five repository sources, `scripts/regression-verification.sh` (`packages/db/src/merge-tail.ts:282-308`); the other four are covered collectively by `path.startsWith("scripts/gate-worker/")` (`defenseListReason` at lines 310-319). Because D1 still builds from those files and D3 alone may delete them, the old exact entry and prefix remain during D2. The single exact entry therefore becomes a transitional dual guard, not an untracked `packages/runner/dist` or per-Run path: add exact entries for `packages/runner/scripts/build-runtime-tools.mjs`, `packages/runner/src/workspace.ts`, `packages/runner/src/adapters.ts`, and `packages/runner/src/adapters/runtime.ts`, while retaining `scripts/regression-verification.sh`. Update the inventory assertion in `packages/db/src/merge-tail.test.ts:169-201` so it no longer derives the guarded script solely from backticked `scripts/...` prompt text and instead asserts the old sources plus all four tracked runner-contract paths.
3. Make the existing `docs/runbooks/add-a-project.md` the one Tier 0 / Tier 1 onboarding page. Preserve its literal Tier 0 `POST /projects`, `POST /projects/:id/repos` with `grantAgents: true`, template selection, instantiation, and human-merge flow (`docs/runbooks/add-a-project.md:40-130`). Add a Tier 1 checklist containing project-scoped `--install-full` and verification, the in-Project Repo and effective-assignee grants, exactly one target-repository file (`scripts/merge-gate.sh`), and operator prerequisites: model CLIs and runner-host authentication, authenticated `gh`, `GITHUB_READ_TOKEN`, `RUNNER_GATE_SERVER` plus the target test toolchain, and the installed private merge-executor GitHub App and isolated service. State that Node on the runner and the gate-worker test toolchain are documented prerequisites, not probes, and that Regression, not an in-Run agent, executes the gate. Move A2's synchronization and three-category readiness material from `agents/README.md:7-72` and `docs/install.md:50-124` into this page; reduce those sections to links while preserving their unrelated source-ownership and installation content. Delete no whole documentation file. Change A2's duplicated-document assertions in `scripts/release-docs.test.mjs:23-39,86-109` to assert the full contract only in the add-project page and the links in both reduced sources.
4. Publish `docs/repo-contract/merge-gate.sh` as a standalone Bash reference of a few dozen lines with one clearly marked repository-specific test-command function. It must accept `--expect-head <full-oid>` and `--master <full-oid>`, reject usage with 2 and no verdict, verify the clean worktree and exact stable HEAD before and after the command, and verify that the stated master exists and is an ancestor. A passing command with both pins prints `MERGE GATE: PASS <oid>` and exits 0; a command or precondition failure prints `MERGE GATE: FAIL (<reason>)` and exits 1; a passing manual run without `--master` prints `MERGE GATE: NOT AUTHORITATIVE (master not stated)` and exits 3. If `AGENTOS_RUN_ID` is set without `AGENTOS_RUN_SCOPE_BYPASS=regression-verification`, refuse before the command with `GATE NOT RUN: refused inside Anneal run <id>` and 76, matching the platform refusal (`scripts/merge-gate.sh:158-171`). SIGINT/SIGTERM print `GATE NOT RUN: <reason>` and retain 130/143. These four line forms and primary codes are the public wire format (`scripts/gate-worker/lib.sh:277-292`); Anneal's own gate documents the signal codes and exact final-line rule (`scripts/merge-gate.sh:11-30`). Add `scripts/repo-contract-merge-gate.test.mjs`, which creates a temporary Git repository, substitutes only the marked dummy command, and covers every verdict/code path without invoking Anneal's real gate. The add-project page must explain which function a target repository replaces, every invariant above, and that the dispatcher supplies both OIDs, so R12 can reproduce `scripts/merge-gate.sh` without reading Anneal source.

Out of scope: deleting any repository copy of `regression-verification.sh`, `gate-dispatch.sh`, `lib.sh`, `mirror-push.sh`, or `remote-gate.sh` (D3); changing what Anneal's gate actually runs; fixing the `DEFENSE_EXACT` project-scope leak; runner claim behavior; the proof-slot semaphore or per-workspace test scripts (chains S and T); `Repo.dependencyProvisioning` (chain C); any change to word-factory.

Constraints: Fail loudly; an unset or empty `AGENTOS_TOOLS` must never fall back to a checkout path. D2's own Regression step runs on the deployed D1 runner against the database's pre-D2 template, so all five repository sources must remain executable and discoverable through merge. Use append-only delivery: no rebase, reset, or force-push. Register the reference artifact and its test by exact path in both publication authorities: add `docs/repo-contract/merge-gate.sh` and `scripts/repo-contract-merge-gate.test.mjs` to `public-snapshot.json`, and add an exact two-file inventory assertion in `scripts/public-snapshot-scan.test.mjs` (the scanner requires one classification per tracked path at `scripts/public-snapshot-scan.mjs:93-143`, and its closed-surface test requires zero unclassified files at `scripts/public-snapshot-scan.test.mjs:395-439`). Retain the existing exact registration for `docs/runbooks/add-a-project.md` (`public-snapshot.json:46-75`; `scripts/operator-api-docs.test.mjs:158-190`). Do not add the executable reference to the prose-only `FAST_DOCUMENTS` list (`scripts/merge-gate-profile.mjs:5-24`); it must select the full gate.

Acceptance:
1. Both canonical Markdown prompts contain only the three quoted `AGENTOS_TOOLS` invocations, and a focused test executes the resolver with `AGENTOS_TOOLS` unset and empty, observes a nonzero status and the named required-variable error, and proves no checkout copy was invoked.
2. Canonical transition tests prove both prompt-only generations' outgoing/successor digests, one-time rollover, idempotent resync, drift refusal, old-chain retention, and new-chain adoption; the focused database sync test exercises that transition through `sync-canonical-prompts` end to end.
3. `defenseListReason` returns a non-null machinery reason for the five retained script-source paths and each of the four new tracked runner-contract paths; the inventory test fails if any is removed, while the gate-worker prefix behavior remains unchanged.
4. `docs/runbooks/add-a-project.md` contains mechanically labeled Tier 0 and Tier 1 checklists, names only `scripts/merge-gate.sh` as a target-repository Tier 1 file, and contains the complete reference-gate reproduction contract. Tests prove `agents/README.md` and `docs/install.md` link to it and no longer contain duplicated readiness checklists.
5. `scripts/repo-contract-merge-gate.test.mjs` asserts exact, color-insensitive final verdict lines and statuses 0, 1, 3, 76, 130, and 143 against dummy commands, plus usage 2 with no verdict, mismatched `--expect-head`, invalid/non-ancestor `--master`, dirty-before, HEAD drift, and dirty-after failures.
6. The snapshot test proves both new exact publication entries exist and classify as included, the existing add-project entry remains included, and `npm run test:snapshot-scan` passes with zero unclassified tracked files.
7. `npm run lint`, `npm run test:snapshot-scan`, and `scripts/merge-gate.sh --expect-head <exact-implementation-oid>` are green, with the final gate line exactly `MERGE GATE: PASS <exact-implementation-oid>`.
