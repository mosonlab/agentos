The five runner-supplied platform scripts have exactly one home inside the runner package that ships them, `scripts/` carries none of them, and no reference to the old `scripts/` paths survives, while clean builds, Runs, gates, snapshots, and releases retain the exact runner-owned tooling.

Route: implementation=senior-dev - deleting the scripts on an under-deployed host silently removes the platform's own regression and gate path, and a surviving reference is invisible until a Run fails

Operator correction (2026-09-01, unattended delegation): the first draft of this brief said to delete the five files outright. That is not implementable and the correction is factual, not a re-scoping of R6. Two facts from the merged tree decide it. First, D1's builder copies the bundle **from the repository sources**: `RUNTIME_TOOL_FILES` names `scripts/regression-verification.sh` and the four `scripts/gate-worker/*.sh` as its `source` entries (`packages/runner/scripts/build-runtime-tools.mjs:12-18`), and the runner's `build` script runs that builder, so deleting the sources fails `npm run build -w @anneal/runner` and therefore the gate's own `build (all workspaces)` step - this chain could never pass its own gate. Second, `scripts/merge-gate.sh:167-168` sources `${SCRIPT_DIR}/gate-worker/lib.sh` and `scripts/gate-worker/step-engine.sh:5-6` requires its caller to have sourced `lib.sh` first; both survive D3, and both run on the gate worker, which is not an Anneal Run and has no `AGENTOS_TOOLS`. So R6's "delete the repository copies" is executed as a **move to a single home inside the runner package**, `packages/runner/runtime-tools/`, mirroring the bundle's existing layout (`regression-verification.sh` plus `gate-worker/`). `scripts/` ends with none of the five; nothing gains a second copy; no fallback path is introduced.

Background: D1 made the runner build an explicit five-file bundle (`packages/runner/scripts/build-runtime-tools.mjs:7-18,52-55,111-152`) and materialize it outside each checkout (`packages/runner/src/workspace.ts:176-202,225-278`). The runner package still invokes that builder on every clean build and its focused tests (`packages/runner/package.json:24-30`); therefore deleting only the old sources would break both the merge gate's clean build and later source-built deployment artifacts. D2 then made both Regression templates require `AGENTOS_TOOLS` with no checkout fallback (`agents/templates/direct-engineer-workflow/06-regression-verification.md:14-30`; `agents/templates/compound-engineer-workflow/10-regression-verification.md:14-30`) and documented `scripts/merge-gate.sh` as the sole target-repository Tier 1 file (`docs/runbooks/add-a-project.md:151-172,212-216`). D3 is safe only after that D2 build is deployed. Its own Regression step executes on the deployed runner, so an older host would still resolve the tooling through the checkout paths this chain vacates. The API exposes the deployed clean identity as `/version.buildSha` (`packages/api/src/version.ts:18-46`), and the existing deployment verifier already treats an unreachable, dirty, wrong-package, or wrong-commit service as failure (`packages/build-info/verify-dist.mjs:3-24,118-168`).

Changes:
1. Before the first file mutation, read the deployed API's `buildSha`, require one clean 40-character commit, and run `git merge-base --is-ancestor b2e971c2877ec54ba7a0374080f863cb44e84023 <deployed-buildSha>`. Print both identities. Any malformed, unreachable, unresolvable, or non-descendant result exits nonzero before deletion; there is no checkout-tool fallback. Cover the predicate with D2 as a satisfying input and D1 `7adfea4af2f4914ae47b0383cb52d4d67e4c23cc` as an unsatisfying input.
2. Move exactly `scripts/regression-verification.sh`, `scripts/gate-worker/gate-dispatch.sh`, `scripts/gate-worker/lib.sh`, `scripts/gate-worker/mirror-push.sh`, and `scripts/gate-worker/remote-gate.sh` to `packages/runner/runtime-tools/`, keeping the bundle's own layout (`regression-verification.sh` at the root, the other four under `gate-worker/`). Move the payloads byte-for-byte apart from path strings that must change; do not rewrite what they do. Update the `source` fields of `RUNTIME_TOOL_FILES` (`packages/runner/scripts/build-runtime-tools.mjs:12-18`) to the new home and leave every `destination` unchanged, so the release-local tree at `packages/runner/dist/runtime-tools` is byte-identical before and after this chain and `packages/runner/src/workspace.ts:198-202` needs no change. After the move, `scripts/` contains none of the five and a clean checkout build still produces the exact same bundle.
3. Remove or redirect every old-path reference. Literal hits currently exist in `CONTRIBUTING.md:84`; `docs/adr/0002-coordinate-main-delivery-with-merge-trains.md:214`; `docs/runbooks/gate-worker.md:115,161,181,362-382,399,430,461`; `packages/db/src/canonical-prompt-sync.dbtest.ts:268,343,473`; `packages/db/src/merge-tail.ts:285`; `packages/db/src/merge-tail.test.ts:187-207`; `packages/runner/scripts/build-runtime-tools.mjs:13-17`; `packages/runner/scripts/build-runtime-tools.test.mjs:90-103,134`; `packages/runner/src/regression-verification-script.test.ts:9-10`; `packages/runner/src/runner.test.ts:388-393`; `packages/runner/src/workspace.test.ts:20-25`; `public-snapshot.json:128,133-136,155`; `scripts/gate-worker/gate-dispatch.test.mjs:1-2,307-355,787`; `scripts/gate-worker/provision.sh:334,346`; `scripts/gate-worker/run-gate.sh:4,66,158-179`; `scripts/merge-gate.sh:164-168`; and `scripts/merge-train.test.mjs:229-253`. Constructed checkout/sibling references also exist in `packages/db/src/template-sources.test.ts:146-181`, `scripts/gate-worker/gate-env.test.mjs:53-63`, `scripts/gate-worker/gate-worker.test.mjs:48-53,911-966,1035`, `scripts/merge-gate-parallel.test.mjs:34`, and `scripts/merge-train.mjs:95-100`. The five moved files' own self and peer path strings move with them and must be updated to the new home in the same change. Update every survivor so tests exercise runner-owned inputs or self-contained fixtures, never a compatibility copy. The root package scripts remain CI-shaped entry points for the affected gate-worker, auto-deploy, snapshot, and release suites (`package.json:30,34-37`); do not weaken their coverage to make removal pass. The merged `.github` tree has no old-path match and requires no D3 edit.
4. Repoint the two survivors that load `lib.sh` from the checkout, because they run where `AGENTOS_TOOLS` does not exist. `scripts/merge-gate.sh:167-168` must source `lib.sh` from the new runner-package home through a repository-root-relative path, and `scripts/gate-worker/step-engine.sh:5-6` keeps its contract that the caller sourced `lib.sh` first. Neither may consult `AGENTOS_TOOLS`, a release directory, or any fallback: the gate worker checks out the candidate and runs the repository's own gate with no runner environment. Prove this by running the real gate on the implementation head, which is Acceptance 6 and is the only witness that reaches this path.
5. Preserve the worker-install and operator surface. `scripts/gate-worker/provision.sh` stays because it provisions the worker host before a checkout exists (`:3-13,47-60`). `scripts/gate-worker/run-gate.sh` stays because it is installed beside each worker mirror and executes the candidate's repository gate (`:3-30`). `scripts/gate-worker/bench-dbtest-concurrency.sh` and `scripts/gate-worker/bench-postgres.sh` stay because they are operator benchmarks over fixed commits (`:3-27` and `:3-26`). `scripts/gate-worker/step-engine.sh` also stays because it travels with the candidate and is sourced by the repository gate, not installed as runner tooling (`:1-21`). The `gate-env` module and gate-worker tests stay as fixture support.
6. Close the publication and release surfaces. Repoint, do not remove, the five whitelist rows in `public-snapshot.json` (`scripts/gate-worker/gate-dispatch.sh`, `scripts/gate-worker/lib.sh`, `scripts/gate-worker/mirror-push.sh`, `scripts/gate-worker/remote-gate.sh` and `scripts/regression-verification.sh`) to the new `packages/runner/runtime-tools/` paths, keeping each row's purpose text: the files still exist and ship publicly, so deleting the rows would fail `npm run test:snapshot-scan` on unlisted files rather than satisfy it. Retain unchanged the entries for the two benches, the provisioner, the worker harness, the step engine and the gate-worker tests. Keep the exact five files only under `packages/runner/dist/runtime-tools`, which remains part of snapshots and required deploy artifacts (`scripts/deploy/release-snapshot.mjs:26-34,123-140`; `scripts/deploy/release-artifacts.mjs:6-15`). Make `scripts/deploy/release-directory.test.mjs:46-69,137-142`, `scripts/deploy/release-snapshot.test.mjs:44-69,88-151`, and `scripts/deploy/quiet-window-deploy.test.mjs:192-201,602-629` use runner-owned or fixed fixture bytes, assert the exact five-file inventory, and never consult deleted sources. Preserve the release-snapshot rule that no runtime-tool tree exists outside runner dist (`scripts/deploy/release-snapshot.test.mjs:137-151`). The release/deploy verifier (`scripts/deploy/release-artifact.mjs:56-79`) must reject a missing, extra, non-regular, or misplaced runtime tool rather than accepting an internally consistent but incomplete release.

Out of scope: Any behavior change to what the five scripts do; the broader `DEFENSE_EXACT` leak recorded as R8 beyond removing obsolete old-path entries; runner claim behavior; the proof-slot semaphore and per-workspace test scripts; `Repo.dependencyProvisioning`; and word-factory.

Constraints: This is the removal-only phase: no compatibility copies, aliases, fallback path, or rewrite of the moved script payloads. Fail loudly. Remove every snapshot whitelist entry for a deleted path in the same change, and keep `npm run test:snapshot-scan` green. Do not rebase or force-push; published history remains append-only.

Acceptance:
1. The pre-delete deployment check has a focused test proving D2/deployed-D2 passes and D1/under-deployed fails nonzero before any deletion action; the real implementation record shows the deployed `buildSha` and successful D2 ancestry check.
2. `git ls-files --error-unmatch` fails for each of the five old `scripts/` paths and succeeds for each of the five under `packages/runner/runtime-tools/`, while a clean runner build produces exactly `regression-verification.sh` plus `gate-worker/gate-dispatch.sh`, `gate-worker/lib.sh`, `gate-worker/mirror-push.sh`, and `gate-worker/remote-gate.sh` beneath `packages/runner/dist/runtime-tools`, byte-identical to the same build on this chain's base commit.
3. `git grep -n -I -E 'scripts/regression-verification\.sh|scripts/gate-worker/(gate-dispatch|lib|mirror-push|remote-gate)\.sh' -- .` exits 1 with empty stdout; any match is failure. The same search for the new `packages/runner/runtime-tools/` paths is expected to match, and the implementation record must list where.
4. `node --test scripts/deploy/release-directory.test.mjs scripts/deploy/release-snapshot.test.mjs scripts/deploy/quiet-window-deploy.test.mjs` and the focused runner runtime-tool and gate-worker tests pass, including missing/extra runtime-tool rejection.
5. `npm run lint` and `npm run test:snapshot-scan` pass.
6. From the isolated implementation worktree, `scripts/merge-gate.sh --expect-head <exact-implementation-oid>` ends with `MERGE GATE: PASS <exact-implementation-oid>` for that exact commit.
