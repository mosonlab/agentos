The runner's runtime-tools bundle ships the six source scripts byte for byte, a gate that produced no verdict leaves a bounded tail of its log in the regression output, and the one-off source-test proof script is retired.

Route: implementation=senior-dev-luna

Background: This brief is based on `origin/main` at `d2314b91b02937b88b9af22279cfae122a3efe61`. `packages/runner/scripts/build-runtime-tools.mjs` copies the six files listed in `RUNTIME_TOOL_FILES` (`:12-19`) into `packages/runner/dist/runtime-tools/`, but `generatedBytes` (`:30-33`, using `bundledRuntimeToolPrefix` at `:25`, applied at `:87` and `:148`) rewrites every occurrence of `packages/runner/runtime-tools/` in the copied bytes to `scripts/`. The rewrite was added by D3's second automatic repair to satisfy a byte-identical acceptance against the pre-move layout; it now means the shipped bytes differ from the committed sources in a way no reader of either can see, and `packages/runner/scripts/build-runtime-tools.test.mjs:80-82,99-101` encodes that rewrite (card cmtjdgm6605f2mp60kcib7woi). `scripts/deploy/release-snapshot.test.mjs` does not encode the rewrite: its `runtimeToolContents` helper (`:44`) reads the source bytes directly, so it already asserts byte identity against the sources and needs no change.

In `packages/runner/runtime-tools/regression-verification.sh`, `finalize` writes the gate dispatch output to a `mktemp` file (`:387-395`), retries exit 75 and 76 up to three times (`:397-402`), and when no admissible verdict exists it calls `die` with a one-line summary (`:429-431`); the EXIT trap removes the log (`:43`). A `MERGE GATE: FAIL` keeps a bounded excerpt (`:420-427`), so a gate that failed is recoverable but a gate that never ran is not: on 2026-09-02 run cmtjbzui2045zmp60k1bxom43 hit three exit-76 dispatches and the cause (a missing bundled file) had to be reconstructed from an earlier review (folded from card cmtjd7hu005epmp60pfnb4h1b).

`scripts/verify-test-no-build.mjs` was the one-off proof for PR #375 that workspace tests run from source; nothing invokes it and its only reference is `public-snapshot.json:180` (folded from card cmtjd7hti05epmp60chq3iqlr, Sol verification 2026-09-02).

Changes:
1. Remove `generatedBytes` and `bundledRuntimeToolPrefix` from `build-runtime-tools.mjs`; the bundle is the sources' bytes. Fix the scripts' own self and peer path strings under `packages/runner/runtime-tools/` so each script resolves its siblings relative to its own location (they already run from `$AGENTOS_TOOLS` in a Run and from the checkout on a host), and update `build-runtime-tools.test.mjs` so it asserts byte identity against the sources rather than against a rewritten copy (the `release-snapshot.test.mjs` fixture already does and stays unchanged). The inventory stays exactly six files.
2. In `regression-verification.sh` `finalize`, before the `die` on a no-verdict outcome, print to stdout a bounded tail of the gate dispatch log (at most 60 lines and 4000 bytes, using the same UTF-8-safe truncation the excerpt already uses) under a heading that names the attempt count and the last exit status, so the session output carries the reason. The verdict file is still not written and the exit status is unchanged.
3. Delete `scripts/verify-test-no-build.mjs` and its `public-snapshot.json` entry. The pull request body states that it was the one-off proof for PR #375 and is not to be wired into the gate.
4. Extend `packages/runner/src/regression-verification-script.test.ts` with a no-verdict dispatch (a stub dispatcher exiting 76 three times with distinct output) and assert the printed tail contains the stub's last output, the attempt count and the exit status, and that no output file is persisted.

Out of scope: `packages/runner/runtime-tools/gate-worker/run-gate.sh` content beyond path strings (another chain edits its excerpt logic; re-read the file on the then-current `origin/main` before editing), `scripts/merge-gate.sh`, `scripts/run-scope-guard.sh`, `scripts/host-proof-slot.sh`, `package.json`, `packages/runner/src/**`, `packages/db/**`, `packages/api/**`, the runtime-tools materialisation in the runner, and any change to gate retry counts or exit codes.

Constraints: Fail loudly with no silent fallback: a bundle that would differ from its source by one byte fails the build, as the existing `byte-mismatch` path does. The six-file inventory and the mode bits asserted by the release snapshot stay as they are. The no-verdict tail is diagnostic output only and must not be mistaken for a verdict: it never contains a line beginning `MERGE GATE:` or `GATE NOT RUN:` that the tail did not already contain verbatim.

Acceptance:
1. `RUNNER_WORKSPACE_ROOT="$(mktemp -d)" npm run test -w @anneal/runner` exits 0 (includes `scripts/build-runtime-tools.test.mjs` and `src/regression-verification-script.test.ts` with the new cases).
2. After `npm run build -w @anneal/runner`, `for f in regression-verification.sh gate-worker/gate-dispatch.sh gate-worker/lib.sh gate-worker/mirror-push.sh gate-worker/remote-gate.sh gate-worker/run-gate.sh; do cmp packages/runner/runtime-tools/$f packages/runner/dist/runtime-tools/$f || exit 1; done` exits 0.
3. `npm run test:auto-deploy` exits 0 and `npm run test:snapshot-scan` exits 0 with `scripts/verify-test-no-build.mjs` absent from the tree and from `public-snapshot.json`.
4. `git grep -n 'verify-test-no-build' -- . ':!records'` prints nothing.
5. `npm run lint` exits 0.
6. `scripts/merge-gate.sh --expect-head <exact-implementation-oid>` exits 0 and prints `MERGE GATE: PASS <exact-implementation-oid>` for the implementation commit being delivered.
